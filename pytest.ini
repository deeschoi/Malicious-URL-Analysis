[pytest]
# Repo root so `api.main` and the analysis scripts are importable; `src` so the
# tests still run against a working tree that was never pip-installed.
pythonpath = . src
testpaths = tests
markers =
    network: tests that require outbound network access
filterwarnings =
    ignore::FutureWarning
